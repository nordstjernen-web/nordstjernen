# Per-site HTML parser overrides.
#
# Keys must match a rule id registered in src/compatibility.c. Allowed
# values are "gumbo" (the default), "lexbor", or "default" (use the
# global engine set by ND_HTML_ENGINE / nd_html_engine_set_default).
#
# Missing or empty values mean: do not override.

[html-engines]

google =
duckduckgo =
wikipedia =
aftenposten =
reddit =
